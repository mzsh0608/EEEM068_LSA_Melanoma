experiment_id: B0

seed: 42
fold: 0

model:
  architecture: resnet18
  pretrained: true
  use_metadata: false

data:
  image_size: 224
  batch_size: 32
  num_workers: 4

training:
  epochs: 10
  learning_rate: 0.0001
  weight_decay: 0.0001
  optimizer: adamw
  loss: weighted_bce

evaluation:
  threshold: 0.5
  checkpoint_metric: roc_auc