# Report

The final EEEM068 report is:

- `EEEM068_LSA_Melanoma_Final_Report_Revised.pdf`

No complete LaTeX report source was supplied. Generated LaTeX table fragments
remain under `outputs/final/tables/publication/` and are not a standalone
report source.

Supporting documentation:

- `methodology_notes.md` records the validated split, preprocessing, model,
  optimisation, and metadata-fusion protocol.
- `results_notes.md` records frozen metrics and evidence-bounded comparisons.
- `analysis_notes.md` records threshold, disagreement, subgroup, failure,
  bootstrap, integrity, and Grad-CAM interpretation.
- `assessment_compliance.md` maps final assessment criteria to repository and
  report evidence.

`outputs/final/J1_HANDOFF.md`, `J1B_tables_manifest.json`, and
`J1_COMPLETE.json` are retained historical provenance snapshots from the J1
evidence-freeze milestone. Their phase language and timestamps are intentional
and should not be read as the current submission status. In particular, the
J1B companion hash for `assessment_compliance.md` records its J1-time version;
the marker-facing compliance document was subsequently updated for final
submission without rewriting that historical manifest.
